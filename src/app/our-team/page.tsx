import React from "react";
import OurTeamClient from "./our-team-client";

export const metadata = {
  title: "Our Team | CEBAR Group",
  description: "Meet the experts at CEBAR Group. A dedicated team of educators, HR specialists, and consultants united by a single purpose.",
};

export default function OurTeamPage() {
  return <OurTeamClient />;
}
