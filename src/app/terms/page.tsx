import React from "react";
import TermsClient from "./terms-client";

export const metadata = {
  title: "Terms of Use | CEBAR Group",
  description: "Terms and conditions for using the CEBAR Group website.",
};

export default function TermsPage() {
  return <TermsClient />;
}
