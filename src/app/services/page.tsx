import React from "react";
import ServicesClient from "./services-client";

export const metadata = {
  title: "Our Services | CEBAR Group",
  description: "CEBAR Group provides top-notch training and HR services across education, corporate, and government sectors — equipping organisations with the tools to excel.",
};

export default function ServicesPage() {
  return <ServicesClient />;
}
